# Nirmal Handloom Backend - Environment Variables
# Copy this file to .env and fill in the values

# ===========================================
# Server Configuration
# ===========================================
PORT=5000
NODE_ENV=production

# ===========================================
# Database Configuration
# ===========================================
# MongoDB Connection String
# Get this from MongoDB Atlas: https://cloud.mongodb.com
# 
# Steps:
# 1. Create a free cluster at MongoDB Atlas
# 2. Create a database user
# 3. Add 0.0.0.0/0 to Network Access (or specific IPs)
# 4. Get connection string from "Connect" > "Connect your application"
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/nirmal-handloom?retryWrites=true&w=majority

# ===========================================
# Authentication
# ===========================================
# JWT Secret - Use a strong random string (64+ characters recommended)
# Generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_here

# ===========================================
# CORS Configuration
# ===========================================
# Comma-separated list of allowed origins (your frontend URLs)
# Include all domains that should access your API
# Example: https://your-frontend.vercel.app,https://www.yourdomain.com
ALLOWED_ORIGINS=https://your-frontend.vercel.app

# ===========================================
# Cloudinary Configuration (Image Uploads)
# ===========================================
# Get credentials from: https://cloudinary.com/console
# 
# Steps:
# 1. Sign up at https://cloudinary.com
# 2. Go to Dashboard
# 3. Copy Cloud name, API Key, and API Secret
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ===========================================
# Razorpay Configuration (Payment Gateway)
# ===========================================
# Get credentials from: https://dashboard.razorpay.com/app/keys
#
# Steps to get Razorpay credentials:
# 1. Sign up at https://razorpay.com
# 2. Complete KYC verification (required for live payments)
# 3. Go to Settings > API Keys
# 4. Generate a new key pair
# 
# For testing: Use Test mode keys (starts with 'rzp_test_')
# For production: Use Live mode keys (starts with 'rzp_live_')
#
# IMPORTANT: Keep the Key Secret secure! Never expose it in frontend code.
RAZORPAY_KEY_ID=rzp_test_your_key_id_here
RAZORPAY_KEY_SECRET=your_razorpay_key_secret_here

